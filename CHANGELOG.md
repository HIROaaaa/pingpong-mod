# 更新日志

版本号规则：`MAJOR.MINOR.PATCH`

- **MAJOR**：不兼容变更（存档格式、网络协议）
- **MINOR**：新内容或行为变更（新方块、物理手感调整）
- **PATCH**：纯修复

---

## [1.9.4] - 2026-09-21

M6/M9 第五轮。用户原话：「球拍还是3D的模型，bendy-lib这个依赖我也装了，人物还是会折，
胳膊也没有弯曲，还是原版直着的状态」，随后「球拍还是一个2D的平面啊，和之前没区别，
我要是的一个3D的模型」。

### 修复：弯曲的真正原因 —— 漏了 `initBend`

上一版查到"没装 bendy-lib 时会静默降级"，但装上网后**还是不会弯**。反汇编
`BendHelper.bend()` 才看清它的完整链路：

```
bend(part, x, y)              → ModelPartAccessor.optionalGetCuboid(part, 0)
                              → mutableCuboid.getAndActivateMutator("bend")
                              → BendableCuboid.applyBend(x, y)
initBend(part, direction)     → mutableCuboid.registerMutator("bend", BendableCuboid.Builder…)
```

也就是说：**名为 `"bend"` 的变形器只有 `initBend()` 才会注册**，光调 `bend()` 时那个
mutator 根本不存在 —— 所以"代码在调 bend、日志干净、却一点不弯"。
现在流程改成：首次见到某个部件时 `initBend(part, Direction.DOWN)`（在关节处弯折），
之后每帧 `bend(part, 角度, 0)`；用弱键表记住已初始化的部件，不重复注册。

### 新增：启动时报告 bendy-lib 状态

原先只在"第一次需要弯曲"时才检测，那要先进世界才看得到结论。现在客户端启动就打日志：
`已检测到 bendy-lib：手臂与躯干会有关节弯曲` / `未检测到 bendy-lib：…不会出现` —— 一条日志定位问题。

### 球拍模型：加厚到 6 像素 + 球面收边 + 换文件名强制刷新

- **厚度 2.2 → 6.0 像素**（约 0.38 格）：真球拍当然没这么厚，但方块世界的物品普遍夸张，
  薄了在任何角度都像一张纸；
- **拍面做两层渐进圆**：外层比内层各缩 0.9 像素，侧看有球面弧度而不是一块等厚的板；
- **元素 6 → 50 个**，圆形轮廓由 9 层拼出；
- **几何换到新文件名 `pingpong_paddle_v2.json`**，`pingpong_paddle.json` 改成只写一行
  `parent` 转发 —— 这是为了**排除客户端资源缓存**这个变量：
  「模型明明改了、玩家却看到旧的」这种情况，换个文件名就能一次性排除。
  （如果这版还看到旧样子，那就不是模型的问题。）

### 验证

- 开发环境装上 bendy-lib 后启动，日志确认 `已检测到 bendy-lib`，模型加载无报错；
- 模型自检：50 元素、坐标不越界、无零尺寸面、厚度 6.0 像素、外层收边到位；
- 7 个套件全绿；产物自检通过。

---

## [1.9.3] - 2026-09-21

M6 收尾 + M9 球拍建模。用户原话：「1.动作还是很僵硬的那种，没有变成平滑的弯曲
2.这次改完先把球拍的3D建模弄好，现在不好看击球效果」。

### 修复：弯曲代码其实一直没生效（重要）

playerAnimator 在**静态初始化时**检测 bendy-lib 是否加载，没装就把 bend 实现换成
`DummyBendable`（空实现）—— 所以我们调 `IBendHelper.bend()` **不报错、但什么都不会弯**。
这正是"加了弯曲代码却没效果"的原因。

- 现在先查 `dev.kosmx.playerAnim.impl.Helper.isBendEnabled()` 再决定是否调用（结果缓存，不每帧反射）；
- 检测不到时在日志里**明确写一行提示**，而不是静默失败；
- ⚠️ **想看到大臂/小臂的弯曲，必须在客户端装 [bendy-lib](https://modrinth.com/mod/bendy-lib)**
  （48KB，客户端 mod）。它是可选依赖：不装不会崩，只是不弯。

### 修复：动作僵硬（两处根因）

1. **相位是顺序切换的**：旧写法 `if (swing<0.35) 只给引拍 / 0.35~0.75 只给前挥 / 之后只给随挥`，
   每段边界上权重是**阶跃**的 —— 动作会"咔"地跳一下。
   现在改成**相邻相位 smoothstep 交叉淡入淡出**（引拍→前挥在 0…0.45 过渡、前挥→随挥在 0.55…1.0 过渡），
   任何时刻都是两段姿态按比例混合，没有阶跃点。
2. **缓动是按帧走的**：旧写法每帧走固定比例，30fps 与 240fps 的收敛速度差 8 倍。
   现在把系数解释成"每 1/20 秒收敛多少"，按**这一帧的实际时长**折算（帧长夹在 4~100ms），
   并把整体速度放缓（引拍约 0.4 秒、前挥约 0.2 秒）。

### M9：球拍模型重建（圆形拍面）

原来的球拍只有 **6 个矩形块**，拍面是长方形 —— 近看还是像块板。现在由
`tools/gen_paddle_model.js` 生成，**32 个元素**：

- **圆形拍面**：原版模型只能画长方体，做不出圆 —— 所以把拍面拆成 **9 层**，
  每层的半宽按圆方程 `w(h) = 0.5·√(d² − h²)` 算出来，叠起来就是十六边形，看着是圆拍面；
- **三明治结构**：红胶皮（0.8 厚）/ 木芯（1.4 厚，横向内缩 0.25 露出拍边木色当倒角）/ 黑胶皮；
- **尺寸按真实球拍**：拍面直径 13 像素（约 0.8 格）、柄 8 像素（约 0.5 格）；
- **手柄三段**：颈部过渡 → 主握 → 底端喇叭口，柄底封一块木头横截面；
- 保留拍面朝向指示条（任何角度都能看出拍面朝哪）。

脚本自带几何自检（坐标不越界 [-16,32]、无零尺寸面、UV 在 32×32 内），
这几条都是"模型整个不渲染/显示紫黑格"的经典原因。

### 验证

7 个套件全绿；dev 客户端启动无异常、模型与贴图加载无报错；产物自检通过。

---

## [1.9.2] - 2026-09-21

M6 第三轮：按实测反馈继续修。用户原话：「1.动作触发时虽然能看到动作，但是玩家的本体不动，
而是多出来一个身体执行动作 2.正反手搓球的动作现在和拉球一样，搓球动作需要改成和现实中搓球一样的
3.反手拉球手臂是从腹部往下引拍然后往上拉的 4.击球点也要相应更改，点位要跟着动画里球拍的位置更改，
不要固定在一个地方」，随后补充「也不是多出来一个身体，其实只有头和头发分离了，头发没有跟着任务动，
并且人物拉球时上半身和腿部是直接折开的，这个也需要变成和手臂一样弯曲的感觉」、
「而且手臂要有弯曲的效果，要能体现的出来大臂和小臂的那种弯曲」。

### 修复：头和头发分离（"多出来一个身体"的真因）

根因：原版 `BipedEntityModel.setAngles` 末尾会把 **hat（头发/帽层）的旋转复制自 head**，
而我们是在那之后才改 `head`，于是两层皮肤错位、头发停在原处不跟头动。
现在 `apply()` 同时接收 `hat` 并施加同一个增量。

### 修复：拉球时上半身和腿部"折开"

躯干原来只是个绕腰**刚体旋转**的方块，弯腰时上半身与腿是"折"而不是"弯"。
现在对 `body` 也用 bend（与手臂同一套机制），弯曲量跟随转体/前倾（最多 26°），腰胯间有了过渡。

### 新增：手臂在肘部弯曲（大臂/小臂的折角）

走 playerAnimator 的 `IBendHelper.bend()` —— 顶点级形变，底层是 **bendy-lib**。
弯曲量 = 基础 24° + 引拍最多 34° + 击球类型修正（搓球压直、拉球更弯），随动作缓动。
**它是可选依赖**：没装 bendy-lib 时 playerAnimator 内部是空实现，动作照常、只是不弯，不会崩
（所以不进 `depends`；想看到弯曲效果请装 bendy-lib）。

### 修复：搓球 / 拉球 / 反手拉球各有各的动作

- **搓球**：原来与拉球共用参数（用户原话「正反手搓球的动作现在和拉球一样」）。现在引拍几乎
  不往后拉、而是**压低**，前挥只往前推一点不抡，躯干略前压贴着台面 —— 像现实里搓球那样推。
- **反手拉球**：用户原话「手臂是从腹部往下引拍然后往上拉的」。正反手拉球现在分成两组参数：
  反手引拍起点更低更贴身（78° / roll −20°），前挥往上收得更陡（−78° vs 正手 −62°）。

### 修复：击球点跟着球拍实际位置走（不再固定）

新增 `util/ArmPose`：把「手型 + 击球类型 + 相位」映射成手臂姿态角度与**球拍中心相对眼睛的位置**。
客户端用它摆动画，服务端（`ModNetworking.swing`）用它定判定点 —— 两端共用同一个函数，
"看到的拍子"与"判定用的拍子"天然一致。实测判定点：待机 +0.13 格、前挥 +0.51~+0.73 格（身前）、
引拍最深 −0.73 格（身后但没出身体范围）。

### 修复：姿态状态按玩家隔离

原来的姿态目标是**全局静态变量**，联机时多个玩家会互相覆写彼此的缓动状态。
现在按 UUID 存（`PingPongModelPose.states`），退出世界时清理。

### 新增验证脚本

- `tools/paddle_point_check.js` —— 算球拍位置，断言判定点合理（在身前、能打到、不穿身体），
  并验证四类击球确实不同（搓球比拉球更靠前更低、削球更高、反手拉球与正手分开）；
- `tools/pose_direction_check.js` —— 重写为从 `ArmPose` 取参数，9 条方向/镜像/弯曲断言。

---

## [1.9.1] - 2026-09-21

M6 第二轮：按三条实测反馈重做动作。用户原话：「动作还是不够，现在只是手臂微微动了一下，
而且我感觉你把正反手的动作弄反了，并且我想要的效果是在蓄力拉球的时候就有手往后引拍的动作的效果」。

### 修复一：动作幅度（"只是微微动了一下"）

第一版给的角度全是 8°~30° 量级，而玩家手臂本身就有 40° 级的原版走路摆动 —— 那点增量
视觉上等于没动。现在以 **0° = 手臂自然下垂**为基准重新标定：

| 相位 | pitch | 说明 |
| --- | --- | --- |
| 持拍待机 | −30° | 手在身前、略抬，球拍举得出来 |
| 引拍（蓄满） | **+60°** | 手拉到**身后偏上**（待机→引拍整整 90°） |
| 前挥 | **−85°** | 手扫到**身前下方**（引拍→前挥 145°） |

手部实测行程：待机→引拍 11.75 像素、引拍→前挥 **12.41 像素**（约 0.78 格）。

### 修复二：正反手改成真镜像

第一版只把侧向分量乘了 ±1，俯仰方向两只手一样，所以"看着像反的、又像没区别"。
现在 `yaw / roll / bodyYaw` 都乘手型符号，且脚本断言**两支手臂的位置关于身体中线严格镜像**
（左右 x 之和 ≈ 0）。

### 新增三：蓄力阶段就有引拍动作

这是用户点出的关键缺失 —— 第一版只有挥拍那 8 tick 有动作，按住左键蓄力时手臂是静止的。
现在**蓄力进度直接驱动引拍**（用 `sqrt` 让前半程就走得明显），松手进入挥拍时从
"已经引好拍"的位置继续前挥，不做瞬间跳变。也就是：
**按住左键 = 手往后慢慢拉 → 松手 = 从后往前甩出去 → 随挥收住。**

### 新增：动作方向的自动化验证（两条脚本，都进套件）

- `tools/pose_solver.js` —— **方向求解器**。反面推：先说清"手该在哪"（待机身前、引拍身后、
  前挥身前），再反解出该给多少度。它当场抓出第一版的方向错误：引拍时手跑到了身前（z=−11.93）、
  前挥时手跑到了身后（z=+8.73）—— 也就是说**上一版把动作做反了**，幅度调多大都救不回来。
  它还暴露了模型的一条硬约束：**手臂只能绕肩旋转，手恒在过肩的竖直面内（x 动不了）**，
  所以横向扫动只能靠躯干转体给，这也正是真人打球要转腰的原因。
- `tools/pose_direction_check.js` —— 8 条方向断言（待机/引拍/前挥的语义、摆幅、镜像），
  改动作常量后必须全绿。两条脚本的常量都从 Java 源码解析，改 Java 自动跟上。

### 验证

- 8 个套件全绿（原 6 条 + 新增的这两条方向脚本）；
- dev 客户端启动无异常，Mixin 正常应用；
- 产物自检通过（版本号 1.9.1、字节码 52、库未被打包）。

---

## [1.9.0] - 2026-09-21

五期 **M6**：顺滑的挥拍动作（落到玩家骨骼上）+ 取消原版攻击动作 + 抛球出手点改到身前。
用户原话：「我需要的是那种顺滑的动作，而且现在左键会先攻击一下然后再蓄力，这个原版攻击的动作不能有，
和任务的胳膊身体可以弯曲的那种动作，和那些动作优化mod一样」以及「这个改完先别提交，
先再把抛球时球抛出去的位置改好，要在玩家前面，现在是在玩家身体里出去的球」。

> 版本号说明：计划里 M6 对应的就是 1.9.0，所以这里从 1.7.0 直接跳到 1.9.0，
> **1.8.0（M8 侧旋与判定点对齐）仍未做** —— 按用户要求先把动作做出来，
> 好让他能看清拍子位置再判断击球问题。

### 新增：action 落到**玩家模型骨骼**上（不再只有拍子自己在飘）

之前的 `PingPongAnimations` 只对「手持物坐标系」做矩阵变换 —— 只有那把拍子在动，玩家手臂纹丝不动，
看起来像"球拍自己飘着"。现在新增 `client/PingPongModelPose` + `mixin/PlayerEntityModelMixin`：

- 注入 `BipedEntityModel.setAngles` 的 **RETURN**（不是 TAIL：这里做的是**加法**，
  若目标方法中途提前 return 会被命中两次 → 每帧叠加、几秒后手臂扭成一团；RETURN 每个出口后只加一次）；
- 用 `instanceof PlayerEntityModel` 收窄范围，僵尸/骷髅等同样用 `BipedEntityModel` 的实体不受影响；
- 四类击球（攻球/弧圈/搓球/削球）× 正反手各有自己的**躯干转体、上臂三轴旋转、头部跟随、
  非持拍手平衡摆**，拍面俯仰也会带动手臂，第三人称一眼能看出拍形；
- **平滑**靠指数缓动，且缓动速度分三段：**引拍慢（0.18）→ 触球快（0.55）→ 随挥缓（0.22）**，
  动作之间连续过渡，不再是原版那种正弦挥手；
- 本地玩家与其他玩家走同一套公式（别人的姿态从 `PaddlePoseCache` 取），所以联机时互相都看得见。

### 新增：playerAnimator 作为前置依赖

- `player-animator` 写进 `fabric.mod.json` 的 `depends`（**非** Jar-in-Jar，用户拍板玩家各自安装）。
  ⚠️ mod id 是 **`player-animator`**（带连字符），不是 `playeranimator`。
- 实测这个库的 13 个 mixin **全在 `client` 段、`mixins: []` 为空** → 纯客户端库，
  **服务端不需要装**（Modrinth 页面标的 `server_side: required` 与代码不符，以代码为准）。
- 1.20.1 线上**只有 beta `1.0.2-rc1`**（查过 Modrinth 全部 1.20.1 版本），没有 release 可选。
- 依赖走**本地 jar**（`libs/player-animation-lib-fabric-1.0.2-rc1+1.20.jar`）：
  实测本机网络下 Modrinth 的 maven 端点一律 404（试过版本号加/不加 `-fabric`、URL 编码 `+`、
  以及 `maven-metadata.xml`），所以不用 maven 坐标。产物里**不会**打包它（`verify_jar` 已确认）。

### 修复：左键不再先播一次原版攻击

`MinecraftClientMixin` 原来只发通知、不取消原版逻辑，于是左键**先抡一次原版挥手**（还带攻击判定）
再进入我们的蓄力 —— 用户说的"先攻击一下"就是这个。现在手持球拍时直接 `cancel`：
不做攻击判定、不播原版挥手动画，视角照旧不动，蓄力与出球完全不受影响
（出球只依赖按键状态机，不看 `doAttack` 的返回值）。

### 修复：抛球出手点改到玩家身前

用户原话：「抛球不要从玩家身上开始抛球，要从玩家视角前面一点，现在发球直接从身体里发出去了，
根本看不见球」。出手点从「正上方 +0.30」改为**「眼睛 + 视线水平方向 × 0.45 格、低 0.15 格」**
（举在面前）；身前被挡住时逐步抬高重试（最多 4 次 ×0.15 格），一路放不下才退回正上方。
**水平速度仍然是严格 0**（纯垂直上抛），出手点前移不等于往前飞 —— 这两条需求不冲突。

### 验证

- 6 个断言套件全绿；`api_diff_scan` 自动更新（`getEyePos` 计数随源码变化）；
- `verify_jar` 通过：版本号一致、新符号在位（含 `PingPongModelPose`）、旧符号 `paddlePoint` 已清除、
  字节码 major = 52（Java 8）；
- **refmap 里确认 Mixin 目标已解析**：`PlayerEntityModelMixin.setAngles` →
  `class_572.method_17087(class_1309;FFFFF)V`（这是"注入是否真的生效"的静态证据）；
- dev 服务端 47 个 mod、dev 客户端 60 个 mod 均正常加载 `player-animator`，无 mixin 冲突。

### 仍需你实测

动作顺不顺、弯曲够不够自然，只有进游戏看才算数。两个已知的观察点：

1. **第一人称能看到拍子挥动**（那套是 `HeldItemRendererMixin` 的矩阵动画，与本次改动独立）；
2. **第三人称/别人看到的**才是这次新增的骨骼动作 —— 按 F5 切视角对比最直观。

---

## [1.7.0] - 2026-09-21

五期 **M7.5**：修掉第二阶段实测反馈里的三个现象（A / B / D）。这一版只碰这三处，
M7.6 的出球距离与蓄力参数、M8 的侧旋与判定点对齐**都没动**。

### 修复

- **现象 A：击球后球"往人物左边偏移一下再出去" → 不再瞬移球**
  `PingPongBallEntity.hitByPaddle` 原来在「球离眼睛 1.5 格以内」时把球
  `refreshPositionAndAngles` 瞬移到 `TableGeometry.paddlePoint`。那个点是
  「前方 0.55 + 侧向 ±0.38 + 上方 0.45」的**固定偏移**，于是每打一拍球都被拖到身体侧前方，
  看起来就是"先横移再飞"。现在只沿出球方向轻推 0.15 格（目的是让球脱离玩家碰撞箱，
  原版 `Entity#move` 会把玩家当墙顶住贴身球），水平位置基本不动。
  这同时是现象 E（击球点位）的一部分根因。

- **现象 B：球拍左偏 / 右偏打出来的球一模一样 → 侧偏真正影响出球方向**
  M3 换接触模型后，侧偏只被喂进 `stroke.worldSwing()` 的**挥拍方向**，
  而挥拍方向的侧向分量被切向摩擦**吸收成自旋**，出球水平方向几乎不变。
  现在拆成两处：`PLAYER_SIDE_MAX_DEGREES = 10°` 直接绕竖直轴旋转**出球速度的水平方向**
  （看得见球被"拨"向哪边），`SWING_SIDE_MAX_DEGREES = 30°` 继续偏转挥拍方向造侧旋。
  偏转必须在**仰角夹紧之后**做 —— 夹紧会用"水平正前方"重设方向，放前面会被整个覆盖掉。

- **现象 D：击球后球上升得有点高 → 基准仰角 30° → 24°**
  `LAUNCH_BASE_SLOW_DEGREES` 是"过网余量 0.12 格"的反解值，偏保守。改 24° 后
  慢速端弧线压低（过网余量约 0.06 格），可行性由 `clampElevation` 兜底，是**只降不涨**的调整。

### 验证

- `tools/contact_model_check.js` 新增第 9 段 5 条断言（⑩~⑭）专门守这两件事：
  侧偏改变出球水平方向、左右方向相反、不改变速度大小与自旋、幅度与侧偏量成正比、
  满侧偏第一跳前横向位移 ≥ 0.25 格（实测 **0.56 格**）。
- 7 个脚本套件全绿；产物 major version 仍为 52（Java 8）；dev 服务端启动无异常。

### 说明：这一版需要你实测

现象 A/B/D 都是**手感**问题，脚本只能验证数值关系。侧偏 10° 是否够明显、弧线是否还偏高，
得你进游戏打两下才知道；觉得不够可以直接调 `PLAYER_SIDE_MAX_DEGREES` 与
`LAUNCH_BASE_SLOW_DEGREES` 两个常量（附注都写在常量注释里）。

---

## [1.6.0] - 2026-09-20

四期 M5 第二步：**源码降到 Java 8 语言级**，为 1.16.5 目标扫清语法障碍。
这一版**没有任何玩法变化**，纯工程改动 —— 但它是多版本移植里最容易被低估的一段。

### 变更（全部是为了 Java 8 兼容）

- **5 个 `record` → 普通不可变类 + getter**：`PingPongPhysics.Surface` / `BounceResult`、
  `PingPongContact.PaddleSurface` / `Result`、`StrokeType.Basis`。
  方法名（`restitution()` / `velocity()` / `forward()` 等）保持不变，调用方零改动。
- **3 处 `switch` 表达式 / 箭头式 `case` → 传统 switch**：
  `StrokeType.flipHand`、`StrokeType` 构造函数的翻译键选择、`ModNetworking.handleAction`、
  `PingPongAnimations.apply`。
- **10 处 `instanceof` 模式匹配 → 显式强转**。

### 新增

- **`tools/java8_check.js` —— Java 8 语法门禁**。扫 27 个源文件，检查
  `record` / `switch` 表达式 / 箭头 case / 文本块 / `var` / `instanceof` 模式匹配 /
  `List.of` 等 Java 9+ 语法与便捷 API。
  这东西当场抓出 15 处问题，其中 **10 处 `instanceof` 模式匹配是我手工检索时漏掉的** ——
  这类"1.20.1 编译通过、切到 1.16.5 才炸"的问题靠人眼查不干净，做成门禁才可靠。

### 为什么不需要装 JDK 8

本机实测只有 `jre1.8.0_421`（JRE，没有 javac）。但 Gradle/Loom 支持**用新版 JDK 编译旧版字节码**：
构建目标设成 `options.release = 8`，由 JDK 17 的 javac 产出 Java 8 字节码，**不必额外安装 JDK 8**。

而且 `options.release = 8` 比"只改 source/target"更严格：它还会**按 Java 8 的 API 做检查**，
误用 `List.of` / `Files.readString` 这类 Java 9+ API 会当场编译失败，而不是等到运行时才炸。
门禁脚本（`tools/java8_check.js`）是第二道网。

**实测验证**（这是本版最关键的证据）：

| 检查 | 结果 |
| --- | --- |
| 产物字节码版本 | `major version: 52` = **Java 8** ✅（改之前是 61 = Java 17） |
| 用本机 JRE 8 直接加载类 | 成功加载，只报 Minecraft 类缺失（`NoClassDefFoundError: net/minecraft/class_243`）——**没有** `UnsupportedClassVersionError` ✅ |
| 1.20.1 服务端加载 Java 8 字节码 | `Done (2.794s)`，正常 ✅（JVM 向下兼容，Java 17 跑 Java 8 字节码没问题） |
| 1.20.1 客户端 | 无模型/Mixin 错误 ✅ |

也就是说：**这一版的 jar 既能被 1.20.1 用，字节码也已满足 1.16.5 的运行时要求。**

---

## [1.5.0] - 2026-09-20

四期 M5 第一步：**版本适配层**（为多版本移植铺路）。

### 说明

- 多版本移植真正的痛苦不是物理或玩法，而是「同一个动作、每个版本写法不同」的地方 ——
  它们差异只有几行，却散落在 20 多个文件里，于是每加一个版本都要满仓库改一遍。
- 新增 `util/Registrar.java`，把注册动作收成一个入口：物品 / 方块 / 实体 / 物品栏。
  业务代码（`ModItems` / `ModBlocks` / `ModItemGroups`）不再直接写
  `Registry.register(Registries.X, ...)`，将来加版本只改 `Registrar` 一处。
- 顺手去掉一处历史遗留：球台以前同时被塞进原版「功能方块」页签和自建「乒乓球」页签，
  现在只出现在自建页签里（物品不会重复出现）。

### 关于 1.16.5 的实情

本机 Gradle 缓存里 Fabric 工具链**只有 1.20.1 那一套**（yarn 1.20.1、fabric-api 0.92.x、loom 1.6.12），
1.16.5 需要的 `minecraft 1.16.5` / `yarn 1.16.5` / `fabric-api 0.42.x` / `loom 0.7.x`
全部不在本地，而本机直连 `services.gradle.org` 与 `maven.fabricmc.net` 受限。
所以「真的编出一个 1.16.5 的 jar」这一步**必须在一次可联网的窗口里做**：
先 `--refresh-dependencies` 把依赖拉进本地缓存，再上 Stonecutter 或独立分支；
Mixin 得按版本各写一份（两个版本的 `Camera.update` / `HeldItemRenderer.renderItem` 签名完全不同）。

---

## [1.4.1] - 2026-09-20

四期 M3 收口：补上「引拍驱动击球点」与「台内前倾」这两条（需求 12、19）。

### 新增

- **击球点随引拍走圆弧**（需求 12）。以前击球点是固定偏移，引拍只是画个动作。
  现在蓄力（= 引拍）进度直接驱动击球点：
  - **正手**：以**肘关节**为圆心、半径 0.45 格画弧，0° → 70°，同时整体后移 0.25·p、下沉，
    并带一点侧向摆动（画圈的手感）；
  - **反手**：半径 0.28 格、0° → 40°，后移 0.12·p —— **幅度明显小于正手**（用户要求 12 原话
    「正手和反手移动的幅度也不一样，正手还会以肘关节为原点画圈移动」）。
  实测（`tools/stroke_point_check.js`）：引拍到底时正手击球点后移 **0.673 格**、下沉 0.18 格、
  侧向摆 0.11 格；反手只后移 **0.300 格**。命中判定用的是同一个点，
  所以不会再出现「看着够到了却没打到」。
- **台内前倾**（需求 19）。眼到台缘的水平距离 < 1.15 格时算「台内」：
  击球点朝球台多伸 **0.25 格**，模型姿态前倾 15°、手臂向台内送出去。
  改的是模型姿态，玩家视角一点不动。

### 说明

- 台内判定在服务端按方块位置精确计算（`TableGeometry.inTable`），客户端渲染用眼高做近似——
  两者都在同一档位（站台边 → 台内），不会出现"服务端以为在台内、画面却没前倾"。

---

## [1.4.0] - 2026-09-20

四期 M3：**把「击球」整个换成真实接触模型**，并做出四类击球各自的动作。
这是四期最大的一次改动 —— 用户列的 9~22 条几乎都由它承载。

### 新增

- **真实接触模型**（`physics/PingPongContact.java`）。旧算法是「按拍面角度查表给自旋」：
  出球速度 = 常数 × 力度、自旋 = 拍面角 × 上限。这在物理上不成立，于是出现了
  「平拍挡上旋球不往下走」「侧旋被平拍挡不往旁边弹」「削球没有『削不住』的概念」这些现象。
  新模型按刚体接触算：

  ```
  u  = v_球 − V_拍                        相对速度
  Jn = −(1+e)·(u·n)·m                     法向冲量（胶皮恢复系数）
  Jt = −min(μ|Jn|, |s|/(1/m+R²/I))·ŝ      切向摩擦冲量（**可能打滑 = 吃不住球**）
  v' = v + (Jn·n + Jt)/m ,  ω' = ω + ((−R·n)×Jt)/I
  ```

  一个公式同时给出用户要的全部现象。离线验证（`tools/contact_model_check.js`）实测：

  | 场景 | 结果 |
  | --- | --- |
  | 上旋球被平拍挡 | vy = **−0.168** → 被压向下（挡弧圈容易下网）✅ |
  | 下旋球被平拍挡 | vy = **+0.168** 且更软（托起来）✅ |
  | 侧旋球被平拍挡 | vz = **−0.168** → 侧向弹 ✅ |
  | 拉弧圈 vs 平击 | 转速 **−0.27 vs −1.64**、出球更快、弧线更低平 ✅ |
  | 削球 vs 搓球 | 下旋 **−2.24 vs −1.06** ✅ |

- **四类击球**（`util/StrokeType.java`）：攻球 / 拉弧圈 / 搓球 / 削球 × 正反手，各自带拍面角、
  挥拍方向、挥拍速度、胶皮类型（普通 / 薄摩擦）。标定见 `tools/calibrate_strokes2.js`。
- **右键 = 搓/削**（需求 17/20b）：右键按住蓄力，松手按蓄力大小选 ——
  **< 57% 搓球、≥ 57% 削球**；左键同理（< 57% 攻球、≥ 57% 拉弧圈）。
  HUD 实时显示「下一拍：搓球/削球/攻球/拉弧圈」。
- **四套挥拍动作**（需求 13/17/21）：攻球水平扫、拉弧圈由下往上刷、搓球放低向前推、
  削球高举往下劈 —— 正反手横向符号相反；第一人称与第三人称（含其他玩家）走同一套渲染。
- **抛球动作**（需求 15/16）：举球时用 `UseAction.BOW` 的手臂姿势，松手抛球时动作同步结束；
  副手拿球、主手拿拍时右键同样能抛，且不会误触发搓球。
- **视线遮挡判定**（需求 20a）：眼睛到球之间被实心方块挡住时**不碰球**，但动作照常播放。
- **力度非线性映射**：挥拍速度 = 基准 × (0.6 + 0.4×力度)。纯线性会让轻打必下网、满打必出台
  —— 球台只有 2.74 格，可用出球速度区间只有 0.30~0.45 格/tick。

### 变更

- 滚轮对拍面角的调整范围从 ±26° 收到 **±12°**：拍面角现在由击球类型给基准
  （攻球 52°、拉球 16°、搓球 30°、削球 37°），滚轮只做微调。
- 姿态广播包新增一个字节（击球类型），**协议不兼容 1.3.0**，双端需同时更新。
- `tools/physics_sanity_check.js` 退役（它验证的是 M3 之前那条已不存在的出球公式）。

### 已知限制

- 接触模型标定基准是「来球 −0.30 带轻上旋」；来球变成强上旋时仍可能下网 ——
  真实乒乓球也有这个现象（吃转），运行时另有"可行性夹紧"兜底。
- 击球点圆弧（引拍时正手绕肘）与"台内前倾"未做，留到下一版。

---

## [1.3.0] - 2026-09-20

四期 M2：把球拍做成真能看出厚度的 3D 模型，并把物品收进自己的创造页签。

### 新增

- **球拍 3D 模型重制（用户需求 1：「球拍还是 2D 的，我要 3D 的模型」）**。
  旧模型其实有 elements，但拍面只有 **0.8 格像素厚**，斜着看就是一张纸片。
  新模型改成**三明治结构**：红胶皮（0.5） / 木芯（1.2） / 黑胶皮（0.5），总厚 2.2 像素，
  木芯比胶皮内缩 0.15 像素，于是拍边会露出一圈木色当倒角；
  另外加了拍肩过渡块、加粗到 2.4 的柄、以及柄底喇叭口（握拍不滑手）。
  贴图集相应从「每象限 8x8」提到 **16x16**（拍面是 12x10 格像素，8x8 画不下颗粒质感）。
- **独立创造模式物品栏（用户需求 6）**：新增 `pingpong` 页签（球拍 / 乒乓球 / 乒乓球台），
  图标用球拍。不再把物品塞进原版「战斗」页签，物品不会在两处重复出现。

### 说明

- **配方不需要改**：现有配方用的是 `{"item": ...}` / `{"tag": ...}` 写法，
  这是 Minecraft 1.13 起的通用格式，**1.16.5 到 1.20.x 全都认**
  （改成 `{"id": ...}` 是 1.21 之后的事）。所以「兼容到 1.16.5」这条在配方层面本来就成立，
  真正的多版本工作（注册表 API、Mixin 签名、Java 版本）留到 M5 做。
- 地胶/16 色已按用户要求取消，不在本版范围内。

---

## [1.2.2] - 2026-09-20

四期 M1：按用户第三轮实测反馈修「打不上台 / 旋转看不出来 / 抛球太高 / 拍形改不动」。
这一版**没有新内容**，全是手感修复与几处真 bug。

### 修复

- **轻点发不出球（真 bug）**：基础拍速 0.30 在球台几何下**物理上过不了网**。
  球台换算成格后：台面高 0.76、球网在 1.97 格外高 0.91、击球点只高出球台 0.23 格 ——
  低速球要在 1.97 格内抬高 0.15 格，必须靠弧线。改为「速度下限 0.28 + 起跳仰角随力度从 30° 收到 14°」
  （`tools/calibrate_hit_speed.js` 反解「过网余量 = 0.12 格」所需仰角，再线性拟合）。
  实测：力度 0~1 六档落点全部落在 2.6~3.0 格（对面台面内），过网余量 0.04~0.16 格。
- **满力度飞出天际**：`MAX_HIT_SPEED` 1.25 → 0.45，`CHARGE_SPEED_BONUS` 0.85 → 0.11。
  旧数值下满力度落点 **9.56 格**（球台全长才 2.74 格），现在 2.93 格。
- **上旋/下旋整体调反（真 bug）**：自旋轴写成 `+tilt`，等于前倾（压着打）给了下旋。
  改为 `-tilt`：前倾 = 上旋 = 马格努斯下压 = 落台前冲；后仰 = 下旋 = 球发飘。
  自旋耦合符号随之修正为「上旋更快、下旋更慢」，并降到 0.10（更大就会把上旋球顶出台）。
- **抛球有向前分量**：`TOSS_DRIFT` 0.06 → 0，生成点从「视线水平前方 0.55 格」改到**玩家正上方**
  （头顶 +0.30 格）。旧写法水平漂移虽小，但平面抛体落回原生成点，球实际是在身前那条竖直线上起落
  —— 看着就是"往前扔出去了"。速度也从 0.34~0.74 改到 0.20~0.32（旧值峰值 1.9~9.1 格，
  用户原话「抛球的默认高度也太高了」）。
- **切回来之后改不了拍形（真 bug）**：客户端状态机丢最后一次滚轮 ——
  `tick()` 只在「手里不是球拍」的分支保存拍形，而切槽位那一 tick 走的正是那个分支，
  且 tick 开头的 `updateSlot()` 已用旧存档覆盖了刚滚出来的值。
  改为滚轮改过值的这一 tick 就落存档。用 `tools/sim_client_state.js` 逐 tick 复现并验证。
- **侧旋只动一下就走直线**：三处叠加 —
  ① 客户端根本不跑马格努斯（只在击球瞬间收包）→ 现在客户端做预测积分；
  ② `ball_motion` 只在击球时发一次 → 自旋球每 2 tick 补一次权威值；
  ③ 贴地 0.85/tick 无差别吃自旋 → 按表面分开（台面 0.96 保留、普通地面 0.85）。
- **跟球跟到旧球**：相机打分从「只比距离」改成「距离 − 新鲜度加成」，刚抛出的球优先；
  抛新球时服务端回收同世界其它球，**一世界一球**。

### 变更（按用户要求）

- **乒乓球物品使用不消耗**：抛球不再扣物品；保留 4 tick 冷却，否则狂点会刷满一屏。
- 拍面角度对出球的影响重新标定：前倾压低弧线、后仰抬高，偏移量 12°。

### 新增（工程侧）

- `tools/calibrate_hit_speed.js` 反解「力度 → 速度 × 起跳仰角」曲线；
  `tools/diag_side_axis.js` 侧旋轴对照实验；`tools/sim_client_state.js` 客户端状态机逐 tick 仿真。
- `tools/physics_sanity_check.js` **改为直接从 Java 源码解析常量** ——
  旧版把手抄常数留在了 0.020/0.85（二期数值），一直在测另一个 mod。
- `tools/pre_push_check.js` + `.git/hooks/pre-commit`：提交前扫凭据/私钥/本机绝对路径。
- `gradle.properties` 钉死 `org.gradle.java.home`，构建不再依赖 JAVA_HOME。

---

## [1.2.0] - 2026-09-20

三期：按用户第二轮实测反馈，做「看得见拍形 + 打得出手感 + 正反手 + 跟球视角」。

### 修复

- **切换物品后拍形归零**：原来客户端 tick 里有一段「手里没球拍就把角度 `*= 0.8`」的逻辑，
  切走再切回来拍面就变平了。改为**按快捷栏槽位分别保存**拍形，切物品、切槽位都原样恢复（需求 2）。
- **换视角看不出球拍朝向**：根因不是模型太扁（模型二期已是真 3D），而是拍面角度只存在于客户端本地、
  且渲染注入只挂在第一人称。现在拍面角度与手型由服务端权威保存并广播
  （`server/PaddlePoseTracker` + `paddle_pose` 包），第三人称与其他玩家手里也能看到正确角度（需求 0）。
- **球速太快、打得太远**：出球基础速度 0.85 → 0.30 格/tick，空气阻力二次项 0.016 → 0.022（需求 1）。

### 变更

- **击球力度由左键按住时长决定**（需求 1）：客户端按下/松手状态机算蓄力，
  出球速度 = `0.30 + 0.85×力度 + 0.25×来球速度`，自旋强度也随力度放大；服务端只做 0~1 范围校验。
- **抛球高度由右键按住时长决定**（需求 5）：改用原版 `getMaxUseTime` + `onStoppedUsing`，
  初速度 0.34~0.74 格/tick（约 2~9 格高），有上限。
- **正反手切换**（需求 4）：新增按键绑定（默认 **C**，可在原版「选项→控制→按键绑定→乒乓球」改键），
  默认正手；切换时拍形回到该手型的准备姿势（正手 tilt −0.25 / side +0.15，反手 tilt −0.35 / side −0.30，需求 4.2）。
- **击球点不跟视角**（需求 4/6）：新增 `util/TableGeometry`，击球点 = 视线前方 + 沿「球台中心 → 玩家」径向朝台外偏移，
  正手偏外侧、反手偏内侧；站在球台两端的两名玩家击球点自然各在一侧。
- **真实挥拍动作**（需求 3）：`client/PingPongAnimations` 三段式（后摆 → 前挥 → 随挥），
  正手从右后向左前扫、反手从左下向正前推，第一/第三人称共用同一套公式。
- 拍面角度视觉幅度加大（俯仰 42°→62°、侧偏 36°→52°），拍面俯仰对出球角的影响加大到 26°。

### 新增

- **跟球视角**（需求 6）：按键绑定（默认 **V**）切换，开启后相机平滑对准最近的球，
  玩家的真实朝向与击球判定完全不受影响（`mixin/CameraMixin`）。
- 球拍模型加**朝向指示片**（竖直插在拍面中央），任何角度都能看出拍面朝哪儿。
- HUD 显示手型、拍面俯仰/侧偏、跟球视角状态与**力度/抛球蓄力条**。
- 网络层新增 `paddle_pose`（S2C 姿态广播）与统一的 `paddle_action`（C2S：拍形/挥拍/蓄力/跟球）。

---

## [1.1.0] - 2026-09-20

二期：按用户实测反馈重修交互与物理，并补上球台与 3D 球拍。

### 修复

- **球不弹跳，只在平地走**：原版 `Entity#move` 在碰撞时会调用 `setVelocity` 清零实体速度，
  导致「落地瞬间的真实速度」被抹掉、反弹永远算不出来。物理速度改为由实体自持
  （`PingPongBallEntity.physicsVelocity`），原版速度只用于追踪同步与渲染。
- **侧旋在地上不侧拐**：纯竖直侧旋轴在水平面上的接触点速度 `ω × r ≡ 0`（数学上恒为零），
  摩擦力无从产生侧向搓动。侧旋轴改为朝行进方向倾斜 40°。
- **球台模型隐身**：模型 elements 坐标写成 0~48 越界（原版模型坐标上限为 `[-16, 32]`），
  客户端报 `JsonParseException` 后模型加载失败。几何体与碰撞箱改为居中到 `-16~32`。

### 变更

- 球拍右键不再生成球（球拍只负责击球）；乒乓球物品右键改为**垂直上抛**，落下时自己调拍形去打。
- 弹性按碰撞面材质区分：球台 0.90 / 球网 0.22 / 球台侧面 0.70 / 其他地面 0.75。
- 出球速度随自旋耦合（上旋更快、下旋更慢），并给出球最小仰角 6°（避免平射贴地滚）。
- 贴地自旋耗散（0.85/tick）与滚动阻力（0.96/tick），否则球会被自身自旋在地面反复搓着乱跑。
- 滚轮步长 0.1 → 0.25（滚 4 格到满值），每次调节在 action bar 弹出百分比。
- 球拍模型改为真 3D（正面红胶皮 / 反面黑胶皮 / 木柄）。

### 新增

- **乒乓球台**：一个方块就是一整张球台（3 格长 × 2 格宽，台面 0.75 格高、中间带网），
  蓝色台面与全部贴图由 `tools/gen_textures.js` 生成。
- 调试开关 `PINGPONG_TRACE=1`：让球每 tick 把位置/速度打进日志，用于核对弹跳轨迹。

---

## [1.0.0] - 2026-09-20

一期初版。

### 新增

- 乒乓球拍物品：左键挥拍击球，**不转动玩家视角**，只有球拍模型在挥。
- 乒乓球实体：服务端权威物理（重力 + 空气阻力 + 马格努斯效应），支持多人联机同步。
- 滚轮控制拍面朝向：滚轮上=后仰（上旋）、滚轮下=前倾（下旋）、Alt+滚轮=左右偏斜（侧旋）。
- 带自旋的刚体弹跳模型（法向冲量 + 切向摩擦冲量），自旋会改变弹跳方向。
- 合成配方、中英文本地化、准星下方 HUD 拍面指示。
