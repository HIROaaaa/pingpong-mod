# pingpong-mod 五期计划（v1.7 → v2.0）

> ### 📌 用户已拍板（2026-09-20，本轮更新）
>
> | 事项 | 结论 |
> | --- | --- |
> | 动作方案 | **用方案 A（playerAnimator + bendy-lib），但不要 Jar-in-Jar 融合进自己的 jar** —— 直接写进 `fabric.mod.json` 的 `depends` 当**普通依赖**。用户原话：「就直接设置到mod需要的依赖里，很多其它mod也都有依赖mod，这很正常」 |
> | 球拍建模 | **先做方案 A（自定义模型加载器）**，C 方案以后再说 |
> | 球的回收表现 | **要一段"飞回来"的动画**（不是直接消失） |
> | 击球范围 | 按**真实球拍尺寸**的比例重设（本鲸娘已查证，见 §8） |
> | 球出球距离 / 蓄力力量 | 都要**再降**（见 §8） |
>
> ### ⏸️ 状态：本轮计划更新后**暂停所有操作**（用户要新开窗口继续项目）
>
> 恢复方式：新窗口直接说「继续 pingpong-mod 五期」，看 §9 的交接说明即可。

> 状态：**计划待确认**，尚未改代码。
> 基线：`main` = `8633fe7`（v1.6.0），四期 M0~M4 已完成，M5 完成"语言级/字节码降级"这一半。
> 本文对应你两轮反馈：**第一阶段 6 条**（§1 表里的 27~32）+ **第二阶段实测 5 个现象**（§7）。
> 旧的 25 条需求与四期里程碑记录在 `docs/plan/v1.3-plan.md`（保留作历史）。

---

## 1. 六条反馈 → 任务映射

| # | 你的要求（原话要点） | 性质 | 归到哪一步 |
| --- | --- | --- | --- |
| 27 | 「我需要的是那种顺滑的动作」；「左键会先攻击一下然后再蓄力，这个原版攻击的动作不能有」；「胳膊身体可以弯曲的那种动作，和那些动作优化 mod 一样」；「我前面要求的所有动作都要做成这种的」 | 动画系统重做 + 抑制原版攻击 | **M6** |
| 28 | 「抛球不要从玩家身上开始抛球，要从玩家视角前面一点，现在发球直接从身体里发出去了，根本看不见球」 | 抛球生成点 | **M7** |
| 29 | 「右键蓄力功能没做」+「侧旋还是不明显，根本体现不出来」 | 两个独立问题：右键蓄力链路 / 侧旋可见度 | **M7 / M8** |
| 30 | 「副手发球后球要消失，乒乓球不要不消耗了，但是等球完全不动了之后要自动回到玩家身上」 | 需求 24 **被撤销** + 新增回收行为 | **M7** |
| 31 | 「球拍还是 2d 的一个平面，我要的是像时装工坊 mod 那样的物品的建模」 | 物品建模升级（第 3 次提这条） | **M9** |
| 32 | 「再写一个计划」 | 本文 | — |

---

## 2. 关于「动作优化 mod」：本鲸娘查到的实情与选型

**先说实话**：本机 Minecraft 的 mods 目录里**只有** `fabric-api` / `malilib` / `worldedit` / `litematica`
（实例 `1.21.5-Fabric_0.16.14`），**没有任何动作类 mod**。所以你是看过别人的效果、想要那种质感，
而不是本机已有可参考的实现。下面是我实查到的两条可行路线（数据来自 Modrinth API，2026-09-20）。

### 路线 A：引入动画库 `playerAnimator`

| 项 | 实查数据 |
| --- | --- |
| 项目 | [playerAnimator](https://modrinth.com/mod/playeranimator)（KosmX，MIT） |
| 下载量 | **2668 万** |
| 支持版本 | 1.16.4 ~ 1.21.7（含 **1.16.5** 与 **1.20.1**）——**同时覆盖我们的两个目标** |
| 1.20.1 版本 | `1.0.2-rc1+1.20-fabric`（beta，181 KB） |
| 依赖 | 可选依赖 [bendy-lib](https://modrinth.com/mod/bendy-lib)（MIT，**client-only**，49 KB，1.20.1 有 4.0.0）——**真正让模型"弯曲"的就是它** |
| 环境 | **client_side: required / server_side: required** |

**结论：用它可以拿到你要的"能弯曲的顺滑动作"，但代价是玩家必须装这个库。**
可以靠 **Jar-in-Jar 打包**做到"玩家只装我们的 mod"，但玩家客户端的下载体积会多 ~230 KB
（playerAnimator 181 KB + bendy-lib 49 KB）——这个量级完全可以接受，四期时我否掉它的理由是
"18 万字节的强制依赖"，现在看**这个理由站不住**（181 KB 不是问题，真正的问题是它需要服务端也装）。

### 路线 B：自写轻量骨骼（不引任何库）

自己给玩家模型加**前臂**（vanilla 只有上臂，没有肘关节，这就是"弯不了"的根本原因），
按击球类型计算一组姿态（躯干 / 头 / 上臂 / 前臂），用平滑插值过渡。
bendy-lib 做的事本质也是这个，只是它做成通用库。

- 优点：**零额外依赖**，玩家只装我们的 mod；完全按乒乓动作定制（不需要通用关键帧系统）
- 缺点：要自己写 Mixin 挂模型部件、自己做插值；只覆盖我们的动作，不能复用别人的动画

### 本鲸娘的建议

**先按路线 B 做**，理由：
1. 你要的动作只有 4 类击球 × 正反手 + 抛球 + 持拍待机，**不是通用动画系统**，自写更贴合；
2. 零依赖在多版本（M5 还要吃 1.16.5）时省掉两套库版本的适配；
3. 万一效果不够顺滑，**随时可以切路线 A**（接口留好：动作只输出一组姿态数值，渲染层换实现即可）。

**这条需要你拍板**——见 §6 的问题 ①。

---

## 3. 各条的实现方案

### 需求 27：顺滑动作 + 抑制原版攻击（工作量最大）

**(a) 抑制原版攻击动作**
现状：`MinecraftClientMixin` 在 `doAttack` 里只**额外**发一个蓄力通知，没有取消原版攻击 ——
所以左键先播一次原版挥臂、再进入我们的蓄力。
改法：在 `doAttack` 的 `@Inject` 里判断「主手是球拍」时 `ci.cancel()`，**只**记录蓄力开始；
同时把原版攻击的"打到方块/实体"的副作用一并去掉（手持球拍时不该有攻击判定）。

**(b) 能弯曲的顺滑动作**
- 给玩家模型注入**前臂**部件（左右各一），构成「肩 → 肘 → 手」两段臂；
- 每个动作输出一组姿态：躯干前倾/侧倾、上臂旋转、前臂弯曲角、手腕角；
- 用**关键帧 + 平滑插值**（不用原版那种正弦挥手），过渡时间随力度变化：
  引拍慢、触球快、随挥缓 —— 这是"顺滑"的关键；
- 四类击球各有自己的关键帧（沿用现有 4 套动作的语义，只是从"矩阵硬编码"改成"骨骼姿态"）。

**(c) 验收**
- 左键按住时**完全没有**原版挥臂；
- 慢放（或截图连拍）能看到手臂在肘部弯曲，而不是整条手臂绕肩旋转；
- 四个动作之间切换不突兀（引拍 → 触球 → 随挥是连续的）。

### 需求 28：抛球从"视角前面一点"出手

现状：抛球生成点已经改成"玩家正上方"（M1 需求 25 为了"纯垂直"），结果**从身体里出来了、看不见球**。
改法：生成点 = 眼睛位置 + **视线水平方向 × 0.45 格**（略低于眼高 0.15），
也就是"举在面前"；同时保留"纯垂直上抛"（水平速度仍为 0）——
注意这两件事不冲突：**生成点前移不代表要有水平速度**。
（旧代码的前移是 `视线前方 0.55 + 水平漂移 0.06`，用户嫌"向前飞"；这次只前移、不动速度。）

### 需求 29：右键蓄力 + 侧旋可见度

**(a) 右键蓄力"没做"**：现在代码里已经有 `useKey` 状态机（`PingPongClient.handleSwing`），
但你实测无效 → 说明链路某处断了。排查清单（按可能性排序）：
1. `client.options.useKey` 在右键按住时会被原版"物品使用"流程消费 → 需要 `@Inject` 到
   `MinecraftClient.doItemUse()` 或 `ClientPlayerInteractionManager.interactItem` 并 cancel；
2. `holdingBallOffhand` 判断把右键整个排除了（副手拿球时）；
3. HUD 的"下一拍"提示没出现 → 说明 `useKeyWasDown` 从未置位。
**先加一个可开关的调试输出**（`PINGPONG_TRACE=1` 时每 tick 打 `useKey.isPressed()` 与蓄力值），
用日志确认断点，再改。

**(b) 侧旋不明显**：三处一起调（都要用脚本先算再改，不手拧）：
- 侧旋轴倾角 60° → 45°（竖直分量从 0.87 降到 0.71，让"落地侧拐"更强）；
- 马格努斯系数 0.003 → 需要重新标定（提高侧向加速度）；
- 球的自旋可视化：球渲染器按自旋轴画一条**旋转痕迹/尾迹**，让"转"这件事肉眼可见。
  （现在只有速度 >1 才撒粒子，几本看不到。）

### 需求 30：撤销"不消耗" + 球静止后回收

- 需求 24（不消耗）**作废** → 恢复 `stack.decrement(1)`（但保留 4 tick 冷却）；
- 新增：球进入"完全静止"状态（`restTicks` 达标）时，**自动飞回投掷者/最后击球者的物品栏**
  （找不到人就原地消失）。需要一个"回收"动画或至少一个音效 + 粒子，否则玩家会以为球凭空没了。

### 需求 31：球拍建模——第 3 次提这条，说明前两次没解决

**前两次的做法与结果**：
- 二期：从"平面 `item/generated`"改成 vanilla `elements` 3D 模型 → 你说"还是平面"；
- 四期 M2：把拍面从 0.8 像素加厚到 2.2 像素（三明治：红胶皮/木芯/黑胶皮）→ 你**还是**说"还是 2d 平面"。

**所以要换思路**。本鲸娘查了时装工坊（FashionCrafter / 4.0 时代的物品建模）那类 mod 的做法，
共同点是：**不是用原版物品模型，而是用自定义渲染器画真实几何体**。
三个可选方案：

| 方案 | 做法 | 效果 | 代价 |
| --- | --- | --- | --- |
| **A（✅ 用户已选）** | Fabric `ModelLoadingPlugin` 注册**自定义 UnbakedModel**，几何体用真正厚的板（拍面 3.0 像素）+ 木芯外露 + 手柄带握把纹路 | 物品栏/手持都是实心厚板，任意角度有体积 | 需要一个模型加载器类 + JSON 指向它 |
| B | 继续加强 vanilla `elements`：拍面拆成"上下两片胶皮 + 中间木芯"三层实体，总厚 3.2 像素 + 四角边条 | 有一定体积感，但仍是盒子拼的 | 纯 JSON，零代码 |
| C（以后再说） | 用实体渲染：手持时在 `HeldItemRenderer` 里画**自定义 3D 网格**（带圆角轮廓的椭圆拍面） | 最接近时装工坊的观感 | 代码量最大，要与现有 Mixin 协调 |

**用户决定：先做 A**（C 以后再说）。A 的实现要点：

1. 实现 `ModelLoadingPlugin`，注册一个自定义 `UnbakedModel` 的加载器（`ModelIdentifier` 指向 `pingpong:item/paddle_model`）；
2. 几何体自己用 `ModelPart` 风格的方块列表拼：拍面做成**椭圆近似**（用 3~4 个不同宽度的板叠出圆角轮廓），
   木芯比胶皮窄，手柄带 2~3 道握把凸起；
3. 物品模型 JSON 只写 `{"loader": "pingpong:paddle"}`，几何体全在代码里 —— 这样"平面感"从根上消失；
4. 保留现有的 `display` 变换（第一/第三人称的手持角度已经调过，别丢）。


---

## 4. 里程碑（⚠️ 已按用户三轮反馈重排，以 §8.4 的表为准）

下面是**最初**的排期，保留作对照；**实际执行顺序见 §8.4**：

| 里程碑 | 内容 | 版本（初版排期） |
| --- | --- | --- |
| **M6** | 抑制原版攻击 + 前臂骨骼 + 关键帧平滑插值（4 类击球 × 正反手 + 抛球 + 待机） | v1.7.0 |
| **M7** | 抛球生成点前移 + 右键蓄力排查与修复 + 撤销"不消耗" + 静止自动回收 | v1.7.1 |
| **M8** | 侧旋可见度（轴倾角/马格努斯重标定 + 旋转尾迹） | v1.8.0 |
| **M9** | 球拍建模升级 | v1.9.0 |
| **M10** | 回到 M5：真的编出 1.16.5 jar | v2.0.0 |

**执行用这张（§8.4）**：

| 里程碑 | 内容 | 版本 |
| --- | --- | --- |
| **M7.5** | 现象 A/B/D：不再瞬移 / 侧偏真正影响出球方向 / 降基准仰角 | v1.7.0 |
| **M7.6** | 出球距离 / 蓄力力量 / 击球范围（§8.3）+ 球"飞回来"的回收动画 | v1.7.1 |
| **M8** | 现象 C/E：侧旋有效性 + 旋转尾迹 + 判定点对齐 | v1.8.0 |
| **M6** | 顺滑动作（playerAnimator + bendy-lib 依赖）+ 抑制原版攻击 | v1.9.0 |
| **M9** | 球拍建模方案 A（自定义模型加载器） | v1.10.0 |
| **M10** | 1.16.5 多版本 | v2.0.0 |

**每个里程碑照旧**：编译 → `runServer` + `runClient` → 脚本套件全绿 → 打 tag → 发 Release。

## 5. 验证方式（沿用四期的方法论）

- 动画/建模这类"看得到"的东西，**用截图对比**而不是嘴说：M6 与 M9 交付时各给一组
  「改动前 / 改动后」的截图（第一人称 + 第三人称 + 物品栏三处）。
- 右键蓄力、侧旋这类"手感"问题，**先加可开关的 trace 输出**定位断点，再改参数；
  参数改动必须过脚本（`contact_model_check.js` / `calibrate_strokes2.js`），不手拧。
- 静止回收要写进 `physics_sanity_check` 的替代脚本里做成断言（静止 tick 数 → 回收）。

## 7. 第二阶段实测反馈（5 个现象）—— 已定位到根因

你报的原话：「球的二跳直接走直线了，根本体现不出来侧旋，击球点位有问题，
而且击球之后球会向人物的左边偏移一下再出去，不是在原位置往前出去，
并且球拍左偏还是右偏球都是这种效果出去，而且球被击中后上升的高度有点高了，也是先写计划先别开始写代码」

**已写诊断脚本**：`tools/ball_trajectory_diag.js`（跑多跳轨迹、把自旋按"上旋/侧旋/沿速度"分解）。

### 现象 A：击球后球会向"人物的左边"偏移一下再出去

**根因（已定位，代码级）**：`PingPongBallEntity.hitByPaddle` 结尾有这么一段：

```java
if (this.getPos().squaredDistanceTo(player.getEyePos()) < 2.25) {   // 1.5 格以内
    this.refreshPositionAndAngles(paddlePos.x, paddlePos.y, paddlePos.z, ...);  // ← 瞬移到拍面点
```

只要球离眼睛不到 **1.5 格**（乒乓球几乎总是这么近），球就被**瞬移**到"拍面点"。
而拍面点固定偏移是：
- 前方 `FORWARD = 0.55`（朝球台）
- 侧向 `SIDE_OUT = 0.38 × sideSign`，而 `sideSign = 正手 ? +1 : −1`
- 高度 `PADDLE_HEIGHT = 0.45`

**"往左"就是这里的侧向分量**：`side = up × outward` 指向**玩家右手**，
正手 `+0.38` 落在右手边；反手 `−0.38` 落在左手边 —— 而用户看到的始终是"往左"，
说明**实际生效的是反手那一支，或者手型没传对**（见现象 B）。

**修法**：击球后**不要瞬移**。改成"沿出球方向推出 0.15 格 + 把球抬到拍面高度"，
保留球原来的水平位置 —— 这样视觉上就是"原地被打飞"，不再横移。

### 现象 B：球拍左偏/右偏，球都是同一种效果出去

**根因**：`sideTilt`（Alt+滚轮）本该同时影响**出球方向**与**自旋**，但 M3 换成接触模型后，
`sideTilt` 只被喂进了 `stroke.worldSwing(basis, sideDegrees)` 的**挥拍方向**，
而挥拍方向的侧向偏转会被接触模型的"切向摩擦"吸收成**自旋**，
于是**出球方向几乎不变**（侧向偏转被摩擦吃掉了）。

**修法**：把 `sideTilt` 拆成两部分 ——
① 一小部分作为**出球方向的偏转**（直接旋转出球速度的水平方向，需求原话"球拍左右偏斜"）；
② 一大部分仍作为挥拍方向的偏转（产生自旋）。
另外要核对 `PlayerHand` 是否真的按 C 键切换（`state.hand` 与 HUD 是否一致）。

### 现象 C：侧旋"根本体现不出来"，二跳走直线

**实测数据**（`node tools/ball_trajectory_diag.js 4.0 0`，侧旋 4 rad/tick）：

| tick | x | 横向 z | 说明 |
| --- | --- | --- | --- |
| 4 | 1.32 | **−0.029** | 飞行中开始侧弯 |
| 8 | 2.54 | **−0.105** | 第一跳前，横向 0.105 格 |
| 12 | 3.29 | **+0.179** | 跳后侧拐换向（自旋方向决定） |
| 20 | 4.66 | **+0.626** | 第二跳前，横向 0.63 格 |

**结论：横向位移是存在的**（0.1 → 0.6 格），但**为什么你看不出来**，有两个真原因：

1. **曲线太"晚"**：第一跳前只有 0.105 格（不到 1 个球径），玩家盯着球看第一段基本看不出弯；
   真正明显（0.6 格）发生在**第二跳之后**，而那时球已经飞出台面、视角里球变小了。
2. **球没有任何"转"的可视反馈**：现在只有自旋模长 > 1 才撒 `CRIT` 粒子，
   而且粒子不带方向 —— 看不出是侧旋还是上旋。

**修法（三处一起）**：
- **提高侧旋的有效性**：`SIDE_AXIS_TILT_DEGREES` 60° → **45°**（竖直分量增大，飞行侧弯更强），
  并把 `MAGNUS_COEFFICIENT` 0.003 重新标定（用 `contact_model_check.js` 的目标：满侧旋第一跳前
  横向 ≥ 0.25 格）；
- **降低自旋衰减**：`SPIN_DECAY` 0.988 → 0.995（30 tick 后自旋保留率从 70% 提到 86%），
  让"第二段也在弯"；
- **加旋转尾迹**：球渲染器按自旋轴画一条**示踪轨迹**（3~5 个残影点，颜色区分上旋/侧旋），
  让"转"这件事肉眼可见 —— 这比继续加大物理数值更有效。

### 现象 D：球被击中后上升高度有点高

**根因**：`PADDLE_HEIGHT = 0.45`（击球点在眼睛上方 0.45 格）+ 接触模型给出的出射仰角。
实测（`contact_model_check.js`）平击 + 力度 0.5 的弧顶是 **1.21 格**（击球点 0.99），
也就是**只比击球点高 0.22 格** —— 单纯看数字不算高。
但你的体感是"高"，所以**先查两件事**：
1. 是不是 `clampElevation` 在来球带自旋时把仰角**抬到了窗口上限**（`LAUNCH_BASE_SLOW = 30°` 偏高）；
2. 是不是"瞬移到拍面点"（现象 A）把球抬到了 `eyeY + 0.45`，接着又以上抛角度飞出去，
   叠加起来显得高。

**修法**：先修现象 A（不再瞬移），再把 `LAUNCH_BASE_SLOW_DEGREES` 30° → 24°，
用 `contact_model_check.js` 复验"落点仍在对方台面内"。

### 现象 E：击球点位有问题（与现象 A 同源）

"击球点位"这个说法在代码里有**两个**含义，都要核：
1. **判定用的击球点**（`TableGeometry.paddlePoint`）：M3 已经改成"引拍驱动圆弧 + 台内伸手"，
   但**服务端判定的那个点**与**客户端画出来的拍面位置**是否一致？
   （第三人称看到拍子在哪 ≠ 服务端认为的击球点在哪 → 玩家会觉得"明明够到了却没打到"）
2. **瞬移的目标点**（现象 A）：它是拍面点，所以"球被吸到拍子位置"也是它造成的。

**修法**：先做一次"可视化对齐" —— 用 `PINGPONG_TRACE=1` 把服务端判定点与球的实际命中位置打进日志，
与渲染的拍面位置对照；确认对齐后再改数值。

### 阶段二里程碑

| 里程碑 | 内容 | 版本 |
| --- | --- | --- |
| **M7.5** | 现象 A（不再瞬移）+ 现象 B（侧偏真正影响出球方向）+ 现象 D（降低基准仰角） | v1.7.0 |
| **M8** | 现象 C（侧旋有效性 + 旋转尾迹）+ 现象 E（判定点与渲染对齐） | v1.8.0 |

（原 M6「顺滑动作」与 M9「球拍建模」仍按 §4 的排期，等 §6 的三个问题拍板后再动。）

---

## 6. 拍板结果（已定，不再是问题）

| # | 当时的问题 | **结论** |
| --- | --- | --- |
| 1 | 动作走哪条路线 | **方案 A：playerAnimator + bendy-lib，作为普通依赖**（不 Jar-in-Jar）。写进 `fabric.mod.json` 的 `depends`，玩家各自安装 —— 用户原话：「就直接设置到mod需要的依赖里，很多其它mod也都有依赖mod，这很正常」 |
| 2 | 球拍建模先做哪一档 | **先 A（自定义模型加载器）**；C（实体渲染）以后再说 |
| 3 | 球的回收怎么表现 | **要"飞回来"的动画**（回到玩家身上），不是就地消失 |

### 依赖落地细节（方案 A）

```groovy
// build.gradle
repositories { maven { url 'https://api.modrinth.com/maven' } }
dependencies {
    modImplementation "maven.modrinth:playeranimator:1.0.2-rc1+1.20"
    modImplementation "maven.modrinth:bendy-lib:4.0.0"
}
```

```json
// fabric.mod.json
"depends": {
  "playeranimator": ">=1.0.2",
  "bendy-lib": ">=4.0.0"
}
```

**注意**：`playerAnimator` 官方要求 **client 与 server 都装**
（Modrinth 上 `client_side: required / server_side: required`），`bendy-lib` 是 client-only。
所以服务端也要装 playerAnimator —— 这点必须写进 README 的安装说明，否则玩家开服会连不上。

---

## 8. 第三阶段反馈（3 条）—— 真实球拍尺寸查证与参数重设

用户原话：「击球后球出去的距离还是太远了，蓄力的基础力量还要降低，击球范围不要那么大，
现实中一个乒乓球拍的大小是多少，你按照差不多的比例来设置击球范围的大小」

### 8.1 真实球拍的尺寸（查证结果）

ITTF 规则只规定「拍面必须是平的、覆盖物厚度 ≤ 4mm 且必须是一整片」，**没有硬性尺寸**，
但比赛与市售球拍有稳定规格：

| 部位 | 真实尺寸 | 换算成 Minecraft 格（1 格 = 1 米） |
| --- | --- | --- |
| 拍面（胶皮部分）直径 | 约 **15 cm** | 0.15 格 |
| 整体长度（含柄） | 约 **25~26 cm** | 0.25~0.26 格 |
| 拍柄长 | 约 **10 cm** | 0.10 格 |
| 整体重量 | 150~200 g | — |

来源：[ITTF 章程/器材规则](https://www.ittf.com/wp-content/uploads/2025/03/ITTF-Statutes2024-JanChapter2-3.pdf)、
[USATT 规则](http://web.ics.purdue.edu/~puttc/CSS_Art/usatt_rules.pdf)、
[乒乓球器材基础知识](https://tyb.tute.edu.cn/info/1261/3447.htm)。

**关键比例**：拍面直径 ≈ 0.15 格 —— 比我们的球（直径 0.28 格）**还小**。
玩家手臂在 MC 里约 0.6~0.7 格，所以"举着拍的击球范围"应该是**手周围 0.3~0.5 格**，
而不是现在的 2.6 格。

### 8.2 击球范围重设

现状：`ModNetworking.HIT_REACH = 2.6`（从击球点算 2.6 格 = 球拍能"够到" 2.6 米，**等于手臂长度的 4 倍**）。

| 方案 | 数值 | 后果 |
| --- | --- | --- |
| 纯真实 | 0.3~0.5 格 | 判定过严：MC 里移动是分步的，容差太小会一直"明明碰到却没打到" |
| **折中（建议）** | **1.15 格** | 仍比真实大 2~3 倍，但从 2.6 收了一半多 |

另外把搜索形状从"以击球点为球心的立方体"改成**沿拍面法线的胶囊/扁盒** ——
球拍是一块板而不是一个点，用球体搜索会在拍子背面也判定命中。

### 8.3 球出球距离与蓄力力量再降

现状（`contact_model_check.js` 实测）：平击力度 0.5 → 落点 **3.0 格**；力度 1.0 → **3.6 格出台**。
球台全场只有 2.74 格，所以 3.0 格确实"飞太远"。

**目标落点带**：轻打落在自己半台到网前（0.9~1.8）、中等力度落对方近台（1.9~2.4）、
满力度压底线（2.4~2.9，允许偶尔出台）。

| 参数 | 现在 | 改为 | 作用 |
| --- | --- | --- | --- |
| `BASE_HIT_SPEED` | 0.30 | **0.24** | 降低基础出球速度 |
| `CHARGE_SPEED_BONUS` | 0.11 | **0.07** | 满力度上限 0.41 → 0.31 |
| `SWING_POWER_FLOOR` | 0.6 | **0.45** | 让"轻点"真的轻（现在轻点也有 60% 挥拍速度） |
| `HIT_REACH` | 2.6 | **1.15** | 击球范围（§8.2） |

改完必须用 `calibrate_strokes2.js` + `contact_model_check.js` 复验：
**轻打不出台、满力度不超底线 0.3 格、四类击球仍都有可行力度档。**

### 8.4 阶段三里程碑

| 里程碑 | 内容 | 版本 |
| --- | --- | --- |
| **M7.5** | 现象 A/B/D（不瞬移、侧偏真正影响出球方向、降基准仰角） | v1.7.0 |
| **M7.6** | 本次 3 条（出球距离/蓄力力量/击球范围，§8.3）+ 球的"飞回来"回收动画 | v1.7.1 |
| **M8** | 现象 C/E（侧旋有效性 + 旋转尾迹 + 判定点对齐） | v1.8.0 |
| **M6** | 顺滑动作（**playerAnimator + bendy-lib 依赖**，§6） | v1.9.0 |
| **M9** | 球拍建模（**方案 A：自定义模型加载器**） | v1.10.0 |
| **M10** | 回到 M5：真的编出 1.16.5 jar | v2.0.0 |


---

## 9. ⏸️ 交接说明（用户要新开窗口继续项目）

**用户要求：本次计划更新后暂停所有操作。** 下面是给下一个窗口的完整交接。

### 9.1 仓库与版本现状

| 项 | 状态 |
| --- | --- |
| 仓库 | `F:\project\pingpong-mod`（GitHub: `HIROaaaa/pingpong-mod`，origin 走 HTTPS） |
| 已发布版本 | v1.0.0 / v1.1.0 / v1.2.0 / v1.2.2 / v1.3.0 / v1.4.0 / v1.4.1 / v1.5.0 / v1.6.0 |
| 当前 `mod_version` | **1.6.0**（下一版从 1.7.0 开始） |
| 构建方式 | `gradle.bat`（路径见 `v1.3-plan.md` §7.0.1）+ `JAVA_HOME=jdk-17.0.3.1` + `--offline`；也可直接用 `mc_gradle` |
| 门禁 | `.git/hooks/pre-commit`（凭据扫描）+ `tools/java8_check.js`（Java 8 语法）；新窗口第一次提交前先跑一次确认环境 |

### 9.2 脚本套件（改物理/参数后必须全跑一遍）

```bat
node tools/java8_check.js          :: Java 8 语法门禁（必须全绿）
node tools/sim_client_state.js     :: 客户端状态机 + 击球类型选择（含 57% 阈值边界）
node tools/contact_model_check.js  :: 接触模型五组手感对照（需求 9/22 的验收）
node tools/calibrate_strokes2.js   :: 八种击球的拍面角/挥拍速度标定
node tools/stroke_point_check.js   :: 击球点圆弧 + 台内伸手
node tools/ball_trajectory_diag.js :: 多跳轨迹 + 自旋分解（诊断“侧旋看不见”用）
node tools/api_diff_scan.js        :: 1.16.5 API 差异扫描（M10 用）
```

### 9.3 下一步优先级（按用户三轮反馈的实际顺序）

1. **M7.5**（现象 A/B/D）：不再瞬移 / 侧偏真正影响出球方向 / 降基准仰角 —— 见 §7 的 A、B、D 节
2. **M7.6**（§8.3 的四个参数 + 球的“飞回来”回收动画）—— 见 §8.2、§8.3
3. **M8**（侧旋有效性 + 旋转尾迹 + 判定点对齐）—— 见 §7 的 C、E 节
4. **M6**（顺滑动作，playerAnimator + bendy-lib 作为**普通依赖**）—— 见 §6
5. **M9**（球拍建模方案 A：自定义模型加载器）—— 见 §3 需求 31
6. **M10**（1.16.5 多版本）—— 见 `v1.3-plan.md` §7.0.3 + `docs/verification/api-diff-1.16.5.md`（46 处差异清单）

### 9.4 M10 的当前卡点（实测，别重复踩）

- **Gradle 7.6.4 分发包只下到一部分**：
  `~/.gradle/wrapper/dists/gradle-7.6.4-bin/f8bb1ab0fb9d2ec84544a03e285572b1/gradle-7.6.4-bin.zip`
  （已下 23.9MB / 共 117MB）。**续传可行**：`HttpWebRequest.AddRange(have)` 能拿到 `PartialContent`。
- **wrapper 自己下不动**：Java 走 Steam++ 时代理会报 `PKIX path building failed`（证书链），
  而 **PowerShell 的 Invoke-WebRequest 能过** —— 下载要用 PowerShell/HttpWebRequest，别指望 `gradlew`。
- **网络间歇性**：`api.github.com` 常年通；`services.gradle.org` / `meta.fabricmc.net` 时通时不通。
  重试通常有效（本轮 GitHub push 也是失败两次后成功）。
- **探针工程已在** `F:\project\pingpong-mod-1165`（独立目录，**不影响** 1.20.1 构建）：
  loom 0.10.66 + gradle 7.6.4 wrapper + yarn 1.16.5+build.10 + loader 0.19.5，
  `src/` 只放了 4 个纯物理文件做探针。分发包补齐后直接在那里跑 `gradlew.bat compileJava` 验证工具链。

### 9.5 别做的事（踩过的坑）

- **不要改现有工程的 `gradle.properties`** 去试 1.16.5 —— 会把能用的 1.20.1 构建搞坏。
- **不要用 PowerShell 的 `Set-Content -Encoding UTF8` 改 `.js`** —— 会加 BOM，Node 解析 shebang 直接报语法错。
- **改 `.js` 里的函数前先完整读一遍文件** —— `function` 重定义不报错、后者静默覆盖前者。
- 断言脚本里**别拿循环残留变量**当被测值，期望与实际都要打印出来。
